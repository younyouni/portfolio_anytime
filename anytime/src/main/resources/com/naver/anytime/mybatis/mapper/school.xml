<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.naver.anytime.mybatis.mapper.SchoolMapper">

	<select id="getSchoolList" resultType="School">
		SELECT name, NVL(cnt, 0) AS total_count
		FROM SCHOOL
		LEFT JOIN (
		SELECT school_id, COUNT(*) cnt
		FROM member
		GROUP BY school_id) b
		ON SCHOOL.school_id = b.school_id
	</select>

	<select id="getSchoolName" parameterType="String" resultType="String">
		SELECT NAME
		FROM SCHOOL
		WHERE domain=#{schoolDomain}
	</select>

</mapper>