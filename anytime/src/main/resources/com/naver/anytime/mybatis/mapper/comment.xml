<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.naver.anytime.mybatis.mapper.CommentMapper">

	<select id="getListCount" resultType="int" parameterType="int">
		select count(*) 
		from comments3
		where board_num = #{board_num}
	</select>

	<select id="getCommentList" resultType="comment" parameterType="int">
	    SELECT 	C.COMMENT_ID, C.USER_ID, C.CONTENT, C.LIKE_COUNT,	  			
		C.COMMENT_DATE, C.RE_LEV, C.RE_SEQ, C.RE_REF, M.NICKNAME	
		FROM COMMENTS C JOIN MEMBER M 
		ON C.USER_ID = M.USER_ID 		
		WHERE C.POST_NUM = #{post_id}
		ORDER BY C.RE_REF ASC, C.RE_SEQ ASC
	</select>
	
	<insert id="commentsInsert" parameterType="comment">
      insert into comments3 
      values(com_seq3.nextval, #{id}, #{content}, sysdate, #{board_num})
   </insert>
   
   <update id="commentsUpdate" parameterType="comment">
   		update comments3
   		set	content=#{content}
   		where num=#{num}
   </update>
   
   <delete id="commentsDelete" parameterType="int">
   		delete comments3
   		where num=#{num}
   </delete>
	
	
</mapper>