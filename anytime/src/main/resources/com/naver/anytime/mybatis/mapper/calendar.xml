<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.naver.anytime.mybatis.mapper.CalendarMapper">
	
	<select id="getCalendarList" resultType="map">
		select *
		from calendar
		where user_id = #{user_id}
	</select>
	
	<insert id="insertCalendarList">
		insert into calendar(CALENDAR_ID, USER_ID, SUBJECT, TYPE, START_TIME, END_TIME, CONTENT, ALL_TIME)
		values(CALENDAR_SEQ.nextval, #{user_id}, #{subject}, #{type}, #{start_time}, #{end_time}, #{content}, #{all_time})
	</insert>
	
</mapper>